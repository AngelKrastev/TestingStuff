package project_1;
import java.util.*;

public class excercise_6 {

	private static Scanner scanner;
	
	public static void main(String args[]) {
		scanner = new Scanner( System.in );
		String s = scanner.next();
		//while(!s.isEmpty()) {
		//	char first = s.charAt(0);
		//	int i=1;
		//	while(i<s.length()) {
		//		if(first>s.charAt(i)) first=s.charAt(i);
		//		i++;
		//	}
		//	System.out.print(first);
		//	s = s.substring(0, s.indexOf(first)-1)+s.substring(s.indexOf(first));
		//s=s.substring(0,s.indexOf("a")-1) + s.substring(s.indexOf("a"));
		System.out.print(s.substring(0,s.indexOf("a")));
		System.out.print(s.substring(s.indexOf("a")+1));
		}
	}
//}
